# MLOps Workspace (placeholder)

Planned structure:
- `datasets/` manifests and quality reports
- `training/` model training pipelines
- `evaluation/` benchmark scripts
- `serving/` model adapter for production inference

Current app is wired to an API contract that allows drop-in replacement of the stub pipeline.
